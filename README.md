First install dependencies:

```bash
npm npm install
# or
yarn
```

To start the development server run:

```bash
yarn dev
#or
npm run dev
```

To start the storybook server run:

```bash
yarn storybook
#or
npm run storybook
```
